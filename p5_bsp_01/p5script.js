var ellipseRadius = 400;
//verkleinert oder vergrößert den Kreis (Radius des Kreises)

function setup() {
   createCanvas(900, 900);
   //Einstellung des Fensters(verkleinern und vergrößern)
}

function draw() {
    ellipse(width / 9, height / 6, ellipseRadius, ellipseRadius);
//Veränderung der Position der Ellipse
}